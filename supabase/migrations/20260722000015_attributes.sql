-- ============================================================================
-- Гибкие поля под профессию: attributes (jsonb).
-- Няне — возраст/образование/медкнижка/возраст детей; водителю — авто/мест/права;
-- домработнице — возраст/проживание/формат. Набор полей на профессию задаётся
-- на фронте (src/lib/fields.ts). Общие поля (имя, город, цена, опыт) — колонки.
-- ============================================================================
alter table public.specialists add column attributes jsonb not null default '{}'::jsonb;
